# Cloudflare API token for DNS challenge
CF_DNS_API_TOKEN=your-cloudflare-api-token

# Email to be used for Let's Encrypt registration
ACME_EMAIL=your-email@example.com

# Host machine directory for Traefik configuration (update with your actual path)
HOST_TRAEFIK_CONFIG=/home/your-username/traefik/config

# Host machine directory for SSL certificates (update with your actual path)
HOST_TRAEFIK_CERTS=/home/your-username/traefik/certs

# Optional: Port for the Traefik dashboard (disabled in production)
DASHBOARD_PORT=8082
